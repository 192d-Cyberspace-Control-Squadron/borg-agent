[project]
name = "borg-agent-docs"
version = "0.1.0"
description = "Zensical site for borg-agent documentation"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "zensical>=0.0.14",
]
